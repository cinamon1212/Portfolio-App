import { defineStyle, TooltipProps } from '@chakra-ui/react'

export const tooltipStyles = defineStyle({
  borderRadius: '4px',
  bg: 'gray.300',
  mb: '6px',
  fontStyle: 'italic',
  fontWeight: 300,
}) as TooltipProps
